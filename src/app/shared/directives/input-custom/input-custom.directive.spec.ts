import { InputCustomDirective } from './input-custom.directive';

describe('InputCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new InputCustomDirective();
    expect(directive).toBeTruthy();
  });
});
